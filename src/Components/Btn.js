import React from 'react'

const Btn = ({texty}) => {
  return (
    <div>    
        <button className='btn' style={{backgroundColor: '#00302E', color : '#FBDDBB'}}>{texty}</button>
    </div>
   
  )
}

export default Btn